# TACHEP-Networking
To activate the environment, run `source agent_venv/bin/activate`

To start the agent software, run `python3 agent.py` Please run this from the root directory of the repository to ensure that output files are saved properly.

See `template.sh` for a template shell script that can be modified to run network tests via Curl commands.
